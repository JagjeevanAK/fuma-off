# OpenAPI Docs Update Configuration
# This file contains configuration for the automated OpenAPI docs update workflow

# Source configuration - customize based on your setup
SOURCE_TYPE=github_repo  # Options: github_repo, url, api_endpoint, local
SOURCE_LOCATION=docs/api/ref  # Path or URL to source docs

# GitHub repository settings (if SOURCE_TYPE=github_repo)
SOURCE_REPO_OWNER=openfoodfacts
SOURCE_REPO_NAME=openfoodfacts-server
SOURCE_BRANCH=main

# URL settings (if SOURCE_TYPE=url)
# SOURCE_URL=https://example.com/docs/api/ref/

# API endpoint settings (if SOURCE_TYPE=api_endpoint)
# API_BASE_URL=https://api.example.com
# API_ENDPOINTS=openapi.yaml,openapi-v3.yaml

# Target settings
TARGET_DIR=ref
BACKUP_ENABLED=true

# Commit settings
COMMIT_MESSAGE_PREFIX="chore: update OpenAPI docs"
COMMIT_AUTHOR_NAME="GitHub Action"
COMMIT_AUTHOR_EMAIL="action@github.com"
